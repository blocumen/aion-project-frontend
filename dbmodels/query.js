// var rawquery = {};
// var q = require("path").join(__dirname,'../dbmodels');

// require('fs').readdirSync(q).filter(function(file){
//     return (file.indexOf(".")!== 0) && (file!== query.js)
// }).forEach(function(file){
//     var qq = require(q +"/"+ file);
//     fileName  = file.split(".")[0];
//     rawquery[fileName] = qq;
// })

// module.exports = rawquery;